{
	"name": "agent-lens-extension",
	"version": "0.1.0",
	"private": true,
	"scripts": {
		"build": "bun run build.ts",
		"watch": "bun run build.ts --watch"
	},
	"devDependencies": {
		"@types/chrome": "latest",
		"esbuild": "latest",
		"typescript": "latest"
	}
}
